import 'package:flutter/material.dart';

class WSpacer extends SizedBox {
  const WSpacer(double size, {super.key}) : super(width: size);
}

class HSpacer extends SizedBox {
  const HSpacer(double size, {super.key}) : super(height: size);
}
